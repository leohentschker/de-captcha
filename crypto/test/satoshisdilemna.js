var Game = artifacts.require("./SatoshisDilemna.sol")

contract('SatoshisDilemna', (accounts) => {
  it("should put 10000 MetaCoin in the first account", async () => {
    const game = Game.deployed()
    assert.equal(game.COLLUDE = 1)
  })
})
